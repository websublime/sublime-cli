{
    "name": "{{ .Name }}",
    "username": "{{ .Username }}",
    "email": "{{ .Email }}",
    "organization": "{{ .Organization }}",
    "token": "{{ .Token }}",
    "id": "{{ .ID }}",
    "expires": "{{ .Expire }}",
    "refresh_token": "{{ .Refresh }}"
}